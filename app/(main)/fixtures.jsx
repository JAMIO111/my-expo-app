import { View } from 'react-native';
import Svg, { Defs, RadialGradient, Stop, Circle } from 'react-native-svg';

const Fixtures = () => {
  return <View className="flex-1 items-center justify-center bg-green-800"></View>;
};

export default Fixtures;
